# Frontend environment — copy to .env.local (never commit .env)

# Production API URL (Render/Railway backend) — required for Vercel production builds
VITE_API_BASE_URL=https://your-api.onrender.com/api/v1

# Google Maps Platform (optional — falls back to OpenStreetMap)
VITE_GOOGLE_MAPS_API_KEY=
